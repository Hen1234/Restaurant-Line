import { state } from "@/store/modules/slot/slot.state";
import { mutations } from "@/store/modules/slot/slot.mutations";
import { actions } from "@/store/modules/slot/slot.actions";

export default {
  namespaced: true,
  state,
  actions,
  mutations,
};
